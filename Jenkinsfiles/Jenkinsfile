pipeline {
  agent {
    docker { image 'unityci/editor:ubuntu-2020.3.9f1-android-0' }
  }
  stages {
    stage('Unit Test') {
      steps {
        sh 'unity-editor -runTests -batchmode -projectPath $WORKSPACE -testResults $WORKSPACE\\results.xml '
      }
    }

  }
}